Kalman filter:

-> Uses hidden variable acceleration
-> Q constructed using discrete white noise variance of which found by sliding values and using one that gave best result
-> Covariance for R (s) chosen in a similar fashion
-> P uses s (used for the measurement noise) and 500( a high value )